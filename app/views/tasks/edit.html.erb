
<h3> Edit your task </h3>

<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :content%>
  <%= f.input :status, :collection => %w[to-do in-progress done] %>
  <%= f.submit %>
<% end %>
