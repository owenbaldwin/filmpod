<div class="grants-container">
  <div class="grants-index">
    <div class="grants-index-mid">




      <div class="grants-index-inner">
        <% if @grants %>
          <h3>Team members:</h3>
          <% @grants.each do |grant| %>
            <%# <%= grant.user.users_dept(@film.id).first&.name</p> %>
            <p><%= grant.user.full_name%> - <%= grant.rank_level%> - <%= grant.user.user_skills%> - <%= grant.department.name%></p>
          <% end %>
        <% else %>
          <p>No members yet</p>
        <% end %>

      </div>
    </div>
  </div>

  <div class="grants-new">
    <div class="grants-new-inner">
      <div class="center">
        <h2> Add members to your team:</h2>
        <%# <% @userdepartment = @grant.user.users_dept(@film.id).first&.name %>
        <%= simple_form_for [ @film, @grant ] do |f| %>
          <div class ="grant-form">
            <div class="form-inputs" data-controller="drag">
              <%= f.association :user, label_method: :full_name, value_method: :id %>
              <%# <%= f.input :user, id: "usersearch"%> %>
                <%# <ul id="userresults"> %>
                <%# </ul> %>
              <%= f.input :department, collection: Department::DPTNAMES %>
              <%= f.input :rank_level, collection: Grant::RANKS%>


            </div>

            <div class="form-actions">
                <%= f.button :submit, "Submit user",  class: "new-film-btn" %>
            </div>
          </div>
        <% end %>
      </div>
      <div class="grants-links">
        <p><%= link_to 'Film homepage', film_path(@film),  class: "to-film-btn" %></p>
      </div>
    </div>
  </div>

</div>
